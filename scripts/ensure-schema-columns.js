/**
 * Legacy helper — schema is applied with `prisma db push` on PostgreSQL.
 * Kept so old deploy paths do not crash if invoked.
 */
console.log('ensure-schema-columns.js: no-op on PostgreSQL (use prisma db push)');
